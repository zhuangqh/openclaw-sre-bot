apiVersion: v1
kind: Secret
metadata:
  name: {{ include "openclaw.fullname" . }}-llm
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  gateway-token: {{ .Values.openclaw.gatewayToken | quote }}
